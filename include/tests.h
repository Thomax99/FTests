#ifndef TESTSH

    #define TESTSH 1
    #define _GNU_SOURCE

    #include "main.h"

    /**
     * @brief make all the tests needed
     * @param args the args needed
     * @return int if all was good or not
     */
    int ftests_test_main(ftests_args_t * args) ;

#endif